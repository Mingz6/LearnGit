name: Build

on:
  push:

jobs:
  deploy-to-integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Demo tests
        run: |
          echo "Run Demo tests - Demo 0 - Trigger123"
